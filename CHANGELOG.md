# 变更记录

本文件记录公开版本的重要变更。版本号沿用语义化版本格式。

## v2.0.0 — 三分类柜首版（2026-08-20）

### 新增

- 本地药品界面从 23 个物理仓位改为三个实体分类柜，同时保留全部 23 项药品的逻辑库存身份。
- 增加药品 ID 到 `cabinet_id=1..3` 的失败关闭映射，以及分类柜清单、药品卡片、详情页和管理员维护界面的本地投影。
- 增加 QSM `/dev/ttyACM0`、115200 8N1 文本协议适配，严格校验 `CABINET n`、`STATUS` 和 `OFF` 的响应与最终状态。
- 纳入 STM32L432KC 三路 WS2812B 固件源码、接线、协议、静态合同和可复现构建说明；不提交设备整片 flash 或生成二进制。

### 改进

- 取药流程改为一次“确认身份并点亮药柜”后自动完成安全核验；无冲突时自动亮灯并进入“还有药吗”，确认页结束后自动执行 `OFF`，不再要求用户重复点击点灯或熄灯，也不把硬件结果描述为柜门自动打开。
- 旧 `slot/control_code` 板端执行请求失败关闭；亮灯结果不确定时禁止自动重试，`OFF` 保持显式、幂等且可复核。
- 启动板端主网关前先确认三柜均为 `STATUS OFF`，避免上次中断留下指示灯常亮。
- 修复板端网关忽略 `SIGCHLD` 时把已成功执行的串口配置误判为失败的问题，并加入该守护进程场景的回归测试。
- 未分配分类柜的历史药品会显示“分类柜待配置”并禁止取药；未知条码不再自动创建无法安全执行的新药档案。

### 兼容边界

- `hardware_slot=1..23`、SQLite 库存身份和取药记录保持不变；三个 `cabinet_id` 只属于本地物理执行与界面投影。
- `zykh_station_app/cloudbase/` 和小程序代码在本版本中没有改动，也不需要因板端适配重新部署云函数或发布小程序。
- v2.0.0 实机摆放清单确认为：1号“日常用药”9 项、2号“外用护理”8 项、3号“慢病处方储备”6 项；S09 双歧杆菌实体放在 3 号柜。
- 小程序药库继续使用 `DAILY/CARE/PRESCRIPTION/COLD = 9/8/5/1`。板端负责把本地 S03 蒙脱石/S13 布洛芬适配为小程序 canonical S13 蒙脱石/S03 布洛芬；S09 对外仍为 `COLD`，不能由此反推实体柜。

### 发布与验证

- 发布前基线 `c6b7bd98d9776455b199bc1133703aba6132d3f3` 与当时的 `origin/main` 一致，并已另存完整 Git bundle、QSM 运行目录静态归档和硬件 flash 快照。
- 自动化验证使用 `QSM_MODE=mock` 与 `DISPENSE_DRY_RUN=true`。当前实机证据只覆盖受控点亮 1 号柜并最终 `STATUS OFF`；2、3 号柜仍须按现场清单分别验收，不能由自动化合同代替。

## v1.9.0 — v1 最终版（2026-08-19）

### 新增

- 完成家属安全事件、一次性配对码、人物范围授权和最小化通知链路。
- 增加药品页自助取药的身份断言、资料完整性、库存、有效期和幂等核验。
- 增加已登记服务对象与访客的问询体征归属、历史查询和 CloudBase 同步契约。
- 记录近似体征与演示补齐的来源、测量质量和完成原因。

### 改进

- 稳定问询药品辅助匹配、观察后方案、固定药仓库存和安全资料迁移。
- 强化板端体征会话、开柜结果未知处理和陈旧响应隔离。
- 统一仓库入口、运行文件边界、验证命令和设备验收文档。

### 清理

- 删除已被现行文档覆盖的 4,995 行历史调试流水账。
- 清除本机临时会话、缓存、旧日志、失效 PID 和未引用截图；保留运行环境、密钥、数据库及业务归档。
- 云端仅保留 `main` 分支；历史正式版本标签与 Release 继续保留，便于审计和回退。

### 发布边界

- `v1.9.0` 是 v1 系列最终版。
- 自动化发布验证使用 `QSM_MODE=mock` 与 `DISPENSE_DRY_RUN=true`，不会触发真实硬件。
- 真实板卡、传感器、摄像头、指纹和开柜场景继续使用独立设备验收记录。

## 历史版本

历史版本和完整提交记录见 [GitHub Releases](https://github.com/DonsonHH/Zykh-QSM/releases)。
